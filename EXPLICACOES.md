import axios from 'axios';

// Consultando o Servidor
axios.get(url);

// Gravar no Servidor
axios.post(url, dados)

// Atualizar no Servidor
axios.put(url, dados)

// Deletar no Servidor
axios.delete(url)
